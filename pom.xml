<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.yak.flink.cdc</groupId>
    <artifactId>yak-flink-cdc-connectors-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Yak Flink CDC Connectors</name>
    <description>Independent sink connectors for Apache Flink CDC Pipeline.</description>
    <url>https://github.com/weifuwan/yak-flink-cdc-connectors</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <modules>
        <module>yak-flink-cdc-connector-jdbc-core</module>
        <module>yak-flink-cdc-connector-jdbc-mysql</module>
        <module>yak-flink-cdc-connector-jdbc-postgres</module>
        <module>yak-flink-cdc-connector-jdbc</module>
        <module>yak-flink-cdc-connector-e2e-tests</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>11</maven.compiler.release>
        <flink-cdc.release>3.6.0</flink-cdc.release>
        <flink-cdc.artifact.version>3.6.0-1.20</flink-cdc.artifact.version>
        <flink.version>1.20.3</flink.version>
        <scala.binary.version>2.12</scala.binary.version>
        <junit.version>5.13.1</junit.version>
        <assertj.version>3.24.2</assertj.version>
        <testcontainers.version>1.21.4</testcontainers.version>
        <mysql.driver.version>8.4.0</mysql.driver.version>
        <postgresql.driver.version>42.7.7</postgresql.driver.version>
        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
        <maven.surefire.plugin.version>3.5.3</maven.surefire.plugin.version>
        <maven.failsafe.plugin.version>3.5.3</maven.failsafe.plugin.version>
        <maven.shade.plugin.version>3.6.0</maven.shade.plugin.version>
        <skipE2E>true</skipE2E>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-common</artifactId>
                <version>${flink-cdc.artifact.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-core</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin.version}</version>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven.failsafe.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>e2e</id>
            <properties>
                <skipE2E>false</skipE2E>
            </properties>
        </profile>
    </profiles>
</project>
